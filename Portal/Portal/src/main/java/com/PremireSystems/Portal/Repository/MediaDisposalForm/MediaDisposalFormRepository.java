package com.PremireSystems.Portal.Repository.MediaDisposalForm;

import com.PremireSystems.Portal.Entity.MediaDisposal.MediaDisposalForm;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MediaDisposalFormRepository extends JpaRepository<MediaDisposalForm,Long> {
}
